---
phase: 01-project-setup
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - .vercel/project.json
autonomous: false
requirements: [LP-04]

must_haves:
  truths:
    - "Vercel Projekt ist mit GitHub Repo verbunden"
    - "Preview Deploy funktioniert"
    - "Website ist unter Preview URL erreichbar"
  artifacts:
    - path: ".vercel/project.json"
      provides: "Vercel Projekt-Konfiguration"
      contains: "projectId"
  key_links:
    - from: "GitHub Repo"
      to: "Vercel Project"
      via: "Git Integration"
      pattern: "auto-deploy on push"
---

<objective>
Vercel Projekt anlegen und Preview Deploy durchfuehren.

Purpose: CI/CD Pipeline von Anfang an etablieren. Jeder Push erzeugt Preview Deploy.

Output:
- Vercel Projekt verknuepft
- Erster erfolgreicher Deploy
- Preview URL funktioniert
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-project-setup/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Git Repository initialisieren und Initial Commit</name>
  <files>
    .git/
    .gitignore
  </files>
  <action>
1. Git Repository initialisieren (falls nicht vorhanden):
   ```bash
   cd /Users/lucaschweigmann/projects/GenerationAI/website
   git init
   ```

2. .gitignore pruefen (sollte von create-next-app existieren):
   - Sicherstellen dass .env*, node_modules, .next, .vercel enthalten sind

3. Alle Dateien stagen und committen:
   ```bash
   git add .
   git commit -m "feat(01): initial project setup with Next.js 16 and Generation AI design system

   - Next.js 16.x with App Router and Turbopack
   - Tailwind CSS v4 with Generation AI color palette (Farbwelt A)
   - Inter font via next/font
   - shadcn/ui base infrastructure
   - Header/Footer layout placeholders
   - Logo assets in public/logos/

   Co-Authored-By: Claude Opus 4.5 <noreply@anthropic.com>"
   ```

4. GitHub Repository erstellen und verknuepfen:
   ```bash
   gh repo create GenerationAI-website --public --source=. --remote=origin --push
   ```
   
   Falls Repo bereits existiert, stattdessen:
   ```bash
   git remote add origin git@github.com:YOUR_USERNAME/GenerationAI-website.git
   git branch -M main
   git push -u origin main
   ```
   
   WICHTIG: Vor Push Rueckfrage an User wegen Repo-Name und Visibility.
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/GenerationAI/website && git log --oneline -1 && git remote -v</automated>
  </verify>
  <done>
    - Git Repository initialisiert
    - Initial Commit mit allen Projekt-Dateien
    - GitHub Remote konfiguriert
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>GitHub Repository Setup</decision>
  <context>
    Bevor wir zu Vercel deployen, brauchen wir ein GitHub Repository.
    
    Aktueller Stand:
    - Projekt ist lokal bereit
    - Git ist initialisiert
    - Initial Commit ist erstellt
  </context>
  <options>
    <option id="new-repo">
      <name>Neues GitHub Repo erstellen</name>
      <pros>Sauberer Start, Generation AI Organisation verwenden</pros>
      <cons>Braucht GitHub Org Access falls vorhanden</cons>
    </option>
    <option id="existing-repo">
      <name>Existierendes Repo verwenden</name>
      <pros>Falls bereits erstellt</pros>
      <cons>Muss Repo-URL angeben</cons>
    </option>
    <option id="manual-later">
      <name>Manuell spaeter</name>
      <pros>Flexibel, kann erst Vercel-Account pruefen</pros>
      <cons>Vercel Deploy muss warten</cons>
    </option>
  </options>
  <resume-signal>
    Waehle: "new-repo", "existing-repo [URL]", oder "manual-later"
    
    Falls new-repo: Soll ich ein Public oder Private Repo erstellen?
    Falls existing-repo: Gib die Repo-URL an (z.B. git@github.com:org/repo.git)
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Vercel Projekt verknuepfen und deployen</name>
  <files>
    .vercel/project.json
  </files>
  <action>
VORAUSSETZUNG: GitHub Remote muss konfiguriert sein (Task 1 + Checkpoint).

1. Vercel CLI ist bereits installiert (global). Projekt verknuepfen:
   ```bash
   cd /Users/lucaschweigmann/projects/GenerationAI/website
   vercel link
   ```
   
   Bei Prompts:
   - Set up: Yes
   - Scope: [Luca's Account oder Generation AI Team wenn vorhanden]
   - Link to existing project: No (neues Projekt)
   - Project name: generation-ai-website
   - In which directory is your code: ./ (default)
   - Want to modify settings: No

2. Preview Deploy durchfuehren:
   ```bash
   vercel deploy
   ```
   
   Output: Preview URL (z.B. generation-ai-website-xxx.vercel.app)

3. Vercel Git Integration aktivieren (automatische Deploys):
   - Vercel Dashboard aufrufen
   - Settings -> Git
   - GitHub Repository verbinden (gleiche Repo wie in Task 1)
   - Production Branch: main
   - Preview Branches: alle anderen

NICHT tun:
- Kein `vercel --prod` ohne explizites OK (Regel aus CLAUDE.md)
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/GenerationAI/website && test -f .vercel/project.json && cat .vercel/project.json | grep -q "projectId" && echo "Vercel linked OK"</automated>
  </verify>
  <done>
    - .vercel/project.json existiert
    - Vercel Projekt ist verknuepft
    - Preview Deploy erfolgreich
    - Preview URL funktioniert
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Vercel Deployment Pipeline:
    - GitHub Repository erstellt/verknuepft
    - Vercel Projekt angelegt
    - Erster Preview Deploy durchgefuehrt
  </what-built>
  <how-to-verify>
    1. Preview URL im Browser oeffnen (aus Vercel Output)
    2. Pruefen:
       - Website laedt (schwarzer Hintergrund)
       - Header mit Logo sichtbar
       - Hero Section mit Headline
       - Farbtest zeigt alle 4 Farben korrekt
       - Footer am Ende
    3. Vercel Dashboard pruefen:
       - Projekt existiert
       - Build erfolgreich (gruener Haken)
       - Git Integration konfiguriert (automatische Deploys)
  </how-to-verify>
  <resume-signal>Type "approved" mit Preview URL, oder beschreibe Issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local -> GitHub | Code push to remote repository |
| GitHub -> Vercel | Build and deploy pipeline |
| Vercel -> Public Internet | Served website |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Public repo | accept | No secrets in Phase 1, .gitignore covers .env* |
| T-03-02 | Tampering | Vercel build | mitigate | Review build output, use lockfile |
| T-03-03 | Elevation of Privilege | Vercel tokens | mitigate | Vercel CLI uses scoped tokens, auto-expires |
</threat_model>

<verification>
Nach Abschluss aller Tasks:

```bash
# Preview URL testen
curl -I [PREVIEW_URL]

# Vercel Status pruefen
vercel ls
```

Preview URL sollte 200 OK zurueckgeben.
</verification>

<success_criteria>
- [ ] GitHub Repository existiert
- [ ] Code ist gepusht (main Branch)
- [ ] Vercel Projekt ist verknuepft
- [ ] Preview Deploy erfolgreich
- [ ] Website ist unter Preview URL erreichbar
- [ ] Vercel Git Integration aktiv (auto-deploy bei Push)
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup/01-03-SUMMARY.md`

Include:
- GitHub Repo URL
- Vercel Project URL
- Preview URL
</output>
