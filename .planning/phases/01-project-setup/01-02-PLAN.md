---
phase: 01-project-setup
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - components.json
  - components/ui/.gitkeep
  - components/layout/header.tsx
  - components/layout/footer.tsx
  - app/page.tsx
  - public/logos/generationai-blau-neon.svg
  - public/logos/generationai-weiss-transparent.svg
autonomous: false
requirements: [LP-04, DS-01, DS-03]

must_haves:
  truths:
    - "shadcn/ui ist initialisiert und bereit fuer Component-Installation"
    - "Header und Footer Platzhalter sind sichtbar"
    - "Logos sind in public/ verfuegbar"
    - "Homepage zeigt strukturiertes Layout"
  artifacts:
    - path: "components.json"
      provides: "shadcn/ui Configuration"
      contains: "tailwind"
    - path: "components/layout/header.tsx"
      provides: "Header Platzhalter"
      exports: ["Header"]
    - path: "components/layout/footer.tsx"
      provides: "Footer Platzhalter"
      exports: ["Footer"]
    - path: "public/logos/generationai-blau-neon.svg"
      provides: "Primary Logo Asset"
  key_links:
    - from: "app/page.tsx"
      to: "components/layout/header.tsx"
      via: "import"
      pattern: "import.*Header"
    - from: "components/layout/header.tsx"
      to: "public/logos"
      via: "Image src"
      pattern: "/logos/"
---

<objective>
shadcn/ui initialisieren, Layout-Struktur anlegen und Projekt-Assets kopieren.

Purpose: Basis-Infrastruktur fuer 21st.dev Components schaffen (Phase 2). shadcn/ui ist die Foundation auf der 21st.dev/ReactBits aufbauen.

Output:
- shadcn/ui konfiguriert (components.json)
- Header/Footer Platzhalter mit Logo
- Logo Assets in public/logos/
- Strukturierte Homepage mit Layout
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-project-setup/01-CONTEXT.md
@.planning/phases/01-project-setup/01-01-SUMMARY.md

<interfaces>
<!-- Von Plan 01 erstellt -->

From lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```

From lib/fonts.ts:
```typescript
export const inter: NextFont;
```

<!-- Tailwind v4 Farben (via @theme in globals.css) -->
- bg-blue-brand, text-blue-brand (#3A3AFF)
- bg-neon, text-neon (#CEFF32)
- bg-black-brand, text-black-brand (#141414)
- bg-gray-light, text-gray-light (#F6F6F6)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: shadcn/ui initialisieren</name>
  <files>
    components.json
    components/ui/.gitkeep
  </files>
  <action>
WICHTIG: 21st.dev und ReactBits sind Component-Registries die auf shadcn/ui aufbauen.
Wir installieren KEINE npm packages fuer 21st.dev - nur shadcn/ui als Basis.

1. shadcn/ui initialisieren (interaktiv):
   ```bash
   cd /Users/lucaschweigmann/projects/GenerationAI/website
   pnpm dlx shadcn@latest init
   ```
   
   Bei Prompts:
   - Style: Default
   - Base color: Neutral (wir ueberschreiben mit eigenen Farben)
   - CSS variables: Yes
   - Tailwind prefix: (none/empty)
   - Global CSS file: app/globals.css
   - CSS variables for theming: Yes
   - Tailwind configuration method: CSS (@theme)
   - Components location: @/components
   - Utils location: @/lib/utils (bereits vorhanden)
   - React Server Components: Yes

2. components/ui/.gitkeep erstellen damit Ordner existiert:
   ```bash
   mkdir -p components/ui
   touch components/ui/.gitkeep
   ```

NICHT tun:
- Keine konkreten shadcn Components installieren (Phase 2)
- Keine 21st.dev Components installieren (Phase 2)
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/GenerationAI/website && test -f components.json && cat components.json | grep -q "tailwind" && echo "shadcn init OK"</automated>
  </verify>
  <done>
    - components.json existiert mit korrekter Konfiguration
    - components/ui/ Verzeichnis existiert
    - Keine globals.css Aenderungen durch shadcn (wir haben eigene)
  </done>
</task>

<task type="auto">
  <name>Task 2: Logo Assets kopieren und Layout-Komponenten erstellen</name>
  <files>
    public/logos/generationai-blau-neon.svg
    public/logos/generationai-weiss-transparent.svg
    components/layout/header.tsx
    components/layout/footer.tsx
  </files>
  <action>
1. Logo-Verzeichnis erstellen und Assets kopieren:
   ```bash
   mkdir -p /Users/lucaschweigmann/projects/GenerationAI/website/public/logos
   cp /Users/lucaschweigmann/projects/GenerationAI/Assets/logos/svg/generationai-blau-neon.svg /Users/lucaschweigmann/projects/GenerationAI/website/public/logos/
   cp /Users/lucaschweigmann/projects/GenerationAI/Assets/logos/svg/generationai-weiss-transparent.svg /Users/lucaschweigmann/projects/GenerationAI/website/public/logos/
   ```

2. components/layout/ Verzeichnis erstellen:
   ```bash
   mkdir -p components/layout
   ```

3. components/layout/header.tsx erstellen:
```tsx
import Image from "next/image";
import Link from "next/link";

export function Header() {
  return (
    <header className="fixed top-0 left-0 right-0 z-50 bg-black-brand/80 backdrop-blur-md border-b border-white/10">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex items-center justify-between h-16">
          {/* Logo */}
          <Link href="/" className="flex items-center">
            <Image
              src="/logos/generationai-weiss-transparent.svg"
              alt="Generation AI"
              width={120}
              height={32}
              priority
            />
          </Link>
          
          {/* Placeholder Navigation - Phase 2 */}
          <nav className="hidden md:flex items-center gap-6">
            <span className="text-gray-light/60 text-sm">Navigation folgt in Phase 2</span>
          </nav>
          
          {/* CTA Placeholder */}
          <div className="flex items-center">
            <span className="bg-neon text-black-brand px-4 py-2 rounded-full text-sm font-medium">
              Beitreten
            </span>
          </div>
        </div>
      </div>
    </header>
  );
}
```

4. components/layout/footer.tsx erstellen:
```tsx
import Image from "next/image";
import Link from "next/link";

export function Footer() {
  return (
    <footer className="bg-black-brand border-t border-white/10 py-12">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex flex-col md:flex-row items-center justify-between gap-6">
          {/* Logo */}
          <Link href="/" className="flex items-center">
            <Image
              src="/logos/generationai-weiss-transparent.svg"
              alt="Generation AI"
              width={100}
              height={26}
            />
          </Link>
          
          {/* Placeholder Links - Phase 2 */}
          <nav className="flex items-center gap-6">
            <span className="text-gray-light/60 text-sm">Links folgen in Phase 2</span>
          </nav>
          
          {/* Copyright */}
          <p className="text-gray-light/40 text-sm">
            &copy; {new Date().getFullYear()} Generation AI
          </p>
        </div>
      </div>
    </footer>
  );
}
```
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/GenerationAI/website && test -f public/logos/generationai-blau-neon.svg && test -f components/layout/header.tsx && test -f components/layout/footer.tsx && echo "Assets + Layout OK"</automated>
  </verify>
  <done>
    - Logo SVGs in public/logos/ vorhanden
    - Header Komponente mit Logo und CTA Platzhalter
    - Footer Komponente mit Logo und Copyright
    - Beide nutzen Generation AI Farben (neon, black-brand, gray-light)
  </done>
</task>

<task type="auto">
  <name>Task 3: Homepage mit Layout-Struktur</name>
  <files>
    app/page.tsx
  </files>
  <action>
app/page.tsx aktualisieren mit Layout-Komponenten:

```tsx
import { Header } from "@/components/layout/header";
import { Footer } from "@/components/layout/footer";

export default function Home() {
  return (
    <>
      <Header />
      
      <main className="min-h-screen pt-16">
        {/* Hero Section Placeholder */}
        <section className="flex flex-col items-center justify-center min-h-[80vh] px-4 text-center">
          <h1 className="text-4xl sm:text-5xl md:text-6xl font-bold text-gray-light mb-6">
            Die KI-Community
            <span className="block text-neon">fuer Studierende</span>
          </h1>
          <p className="text-lg sm:text-xl text-gray-light/70 max-w-2xl mb-8">
            Lerne, vernetze dich und werde Teil der ersten kostenlosen KI-Community im DACH-Raum.
          </p>
          <div className="flex gap-4">
            <button className="bg-neon text-black-brand px-6 py-3 rounded-full font-medium hover:bg-neon/90 transition-colors">
              Kostenlos beitreten
            </button>
            <button className="border border-gray-light/30 text-gray-light px-6 py-3 rounded-full font-medium hover:bg-white/5 transition-colors">
              Mehr erfahren
            </button>
          </div>
        </section>
        
        {/* Color Test Section - Verifies Tailwind v4 Config */}
        <section className="py-16 px-4">
          <div className="max-w-4xl mx-auto">
            <h2 className="text-2xl font-bold text-gray-light mb-8 text-center">
              Design System Test (wird in Phase 2 entfernt)
            </h2>
            <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
              <div className="bg-blue-brand p-4 rounded-lg text-center">
                <span className="text-neon font-medium">blue-brand</span>
              </div>
              <div className="bg-neon p-4 rounded-lg text-center">
                <span className="text-black-brand font-medium">neon</span>
              </div>
              <div className="bg-gray-light p-4 rounded-lg text-center">
                <span className="text-black-brand font-medium">gray-light</span>
              </div>
              <div className="bg-black-brand border border-white/20 p-4 rounded-lg text-center">
                <span className="text-gray-light font-medium">black-brand</span>
              </div>
            </div>
          </div>
        </section>
      </main>
      
      <Footer />
    </>
  );
}
```
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/GenerationAI/website && grep -q "Header" app/page.tsx && grep -q "Footer" app/page.tsx && grep -q "bg-blue-brand" app/page.tsx && echo "Homepage structure OK"</automated>
  </verify>
  <done>
    - Homepage importiert Header und Footer
    - Hero Section mit Headline und CTAs
    - Color Test Section zeigt alle 4 Hauptfarben
    - Responsive Layout (sm, md breakpoints)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Next.js Projekt mit Generation AI Design System:
    - Tailwind v4 mit Farbwelt A (blue-brand, neon, black-brand, gray-light)
    - Inter Font
    - Header/Footer Layout
    - Homepage mit Hero und Farbtest
  </what-built>
  <how-to-verify>
    1. Terminal: `cd /Users/lucaschweigmann/projects/GenerationAI/website && pnpm dev`
    2. Browser oeffnen: http://localhost:3000
    3. Pruefen:
       - Schwarzer Hintergrund (#141414)
       - Header mit Logo sichtbar
       - Hero Section mit gruener "fuer Studierende" Headline
       - Farbtest-Section zeigt 4 Farbboxen korrekt
       - Footer am Ende der Seite
    4. DevTools -> Console: Keine Errors
    5. DevTools -> Network -> Fonts: Inter geladen
  </how-to-verify>
  <resume-signal>Type "approved" wenn alles funktioniert, oder beschreibe Issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shadcn CLI -> local | Component configuration from registry |
| local assets -> public | Logo files from parent directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | shadcn init | mitigate | Review components.json after init, verify paths |
| T-02-02 | Spoofing | Logo assets | accept | Logos from trusted internal source (Assets/logos/) |
| T-02-03 | Information Disclosure | Source files | accept | No sensitive data in Phase 1 components |
</threat_model>

<verification>
Nach Abschluss aller Tasks:

```bash
cd /Users/lucaschweigmann/projects/GenerationAI/website

# 1. Build testen
pnpm build

# 2. Dateistruktur pruefen
ls -la components/
ls -la components/layout/
ls -la public/logos/

# 3. TypeScript Check
pnpm tsc --noEmit
```

Alle Commands sollten ohne Fehler durchlaufen.
</verification>

<success_criteria>
- [ ] shadcn/ui initialisiert (components.json existiert)
- [ ] components/ui/ Verzeichnis existiert
- [ ] Logo SVGs in public/logos/
- [ ] Header und Footer Komponenten funktionieren
- [ ] Homepage zeigt strukturiertes Layout
- [ ] Alle 4 Farbwelt-A-Farben rendern korrekt
- [ ] `pnpm build` erfolgreich
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup/01-02-SUMMARY.md`
</output>
