---
phase: 01-project-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - app/globals.css
  - app/layout.tsx
  - app/page.tsx
  - lib/fonts.ts
  - lib/utils.ts
  - components.json
  - components/ui/.gitkeep
  - components/layout/header.tsx
  - components/layout/footer.tsx
  - public/logos/generationai-blau-neon.svg
  - public/logos/generationai-weiss-transparent.svg
  - .eslintrc.json
  - .prettierrc
  - .gitignore
autonomous: true
requirements: [LP-04, DS-01, DS-03]

must_haves:
  truths:
    - "Dev Server startet ohne Fehler mit pnpm dev"
    - "Tailwind Farben (bg-blue-brand, text-neon, etc.) rendern korrekt"
    - "Inter Font laedt und ist sichtbar"
    - "Homepage zeigt Platzhalter-Content auf schwarzem Hintergrund"
  artifacts:
    - path: "package.json"
      provides: "Projekt-Dependencies"
      contains: "next"
    - path: "app/globals.css"
      provides: "Tailwind + Generation AI Farbwelt"
      contains: "@theme"
    - path: "app/layout.tsx"
      provides: "Root Layout mit Fonts"
      contains: "inter.variable"
    - path: "lib/utils.ts"
      provides: "cn() Helper fuer Class Merging"
      exports: ["cn"]
    - path: "postcss.config.mjs"
      provides: "PostCSS fuer Tailwind v4"
      contains: "@tailwindcss/postcss"
  key_links:
    - from: "app/layout.tsx"
      to: "lib/fonts.ts"
      via: "import"
      pattern: "import.*fonts"
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "import"
      pattern: "import.*globals.css"
---

<objective>
Next.js 16 Projekt mit Tailwind v4 und Generation AI Design System aufsetzen.

Purpose: Fundament fuer alle weiteren Phasen schaffen. Ohne funktionierendes Projekt + Design System kann Phase 2 (Landing Page) nicht starten.

Output: Funktionierendes Next.js Projekt mit:
- Tailwind v4 mit Farbwelt A (blue-brand, neon, black-brand, gray-light)
- Inter Font via next/font
- shadcn/ui Basis-Infrastruktur (cn() helper, components.json)
- Header/Footer Platzhalter-Struktur
- Logo Assets in public/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-project-setup/01-CONTEXT.md
@.planning/phases/01-project-setup/01-RESEARCH.md
@../v1-web-app/Design.md

<interfaces>
<!-- Tailwind v4 Config Pattern (CSS-based, nicht JS) -->

From app/globals.css:
```css
@import "tailwindcss";

@theme {
  --color-blue-brand: #3A3AFF;
  --color-neon: #CEFF32;
  --color-black-brand: #141414;
  --color-gray-light: #F6F6F6;
  --font-sans: var(--font-inter), system-ui, sans-serif;
}
```

From lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```

From lib/fonts.ts:
```typescript
export const inter: NextFont;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pnpm installieren und Next.js Projekt erstellen</name>
  <files>
    package.json
    pnpm-lock.yaml
    tsconfig.json
    next.config.ts
    app/layout.tsx
    app/page.tsx
    app/globals.css
    .eslintrc.json
    .gitignore
  </files>
  <action>
1. pnpm global installieren (Research: nicht vorhanden auf System):
   ```bash
   npm install -g pnpm
   ```

2. Next.js Projekt im aktuellen Verzeichnis erstellen:
   ```bash
   cd /Users/lucaschweigmann/projects/GenerationAI/website
   pnpm create next-app@latest . --typescript --tailwind --eslint --app
   ```
   WICHTIG: KEIN --src-dir Flag verwenden (Research: shadcn erwartet root-level app/)
   
   Bei Prompts:
   - TypeScript: Yes
   - ESLint: Yes
   - Tailwind CSS: Yes
   - App Router: Yes
   - src/ directory: NO (wichtig!)
   - Import alias: @/* (default)
   - Turbopack: Yes (default in Next.js 16)

3. Additional Dependencies installieren:
   ```bash
   pnpm add clsx tailwind-merge framer-motion lucide-react
   pnpm add -D prettier
   ```

4. PostCSS Config fuer Tailwind v4 erstellen (postcss.config.mjs):
   ```javascript
   const config = {
     plugins: {
       "@tailwindcss/postcss": {},
     },
   };
   export default config;
   ```

5. Prettier Config erstellen (.prettierrc):
   ```json
   {
     "semi": true,
     "singleQuote": false,
     "tabWidth": 2,
     "trailingComma": "es5"
   }
   ```
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/GenerationAI/website && pnpm --version && node -e "const pkg = require('./package.json'); console.log('next:', pkg.dependencies.next); console.log('tailwindcss:', pkg.dependencies.tailwindcss)"</automated>
  </verify>
  <done>
    - pnpm installiert und funktioniert
    - package.json existiert mit next, tailwindcss, clsx, tailwind-merge, framer-motion, lucide-react
    - postcss.config.mjs existiert mit @tailwindcss/postcss
    - Keine src/ Directory (app/ ist im Root)
  </done>
</task>

<task type="auto">
  <name>Task 2: Tailwind v4 mit Generation AI Farbwelt konfigurieren</name>
  <files>
    app/globals.css
    postcss.config.mjs
  </files>
  <action>
WICHTIG: Tailwind v4 nutzt CSS-basierte @theme Config, NICHT tailwind.config.js!

1. app/globals.css komplett ersetzen:
```css
/* Generation AI Design System - Tailwind v4 */
@import "tailwindcss";

@theme {
  /* Farbwelt A - Primary (Web-App Default) */
  --color-blue-brand: #3A3AFF;
  --color-neon: #CEFF32;
  --color-black-brand: #141414;
  --color-gray-light: #F6F6F6;
  
  /* Farbwelt B - Secondary (Campaigns, konfiguriert aber nicht aktiv) */
  --color-pink-brand: #FC78FE;
  --color-red-brand: #F5133B;
  
  /* Semantic Aliases */
  --color-background: var(--color-black-brand);
  --color-foreground: var(--color-gray-light);
  --color-primary: var(--color-blue-brand);
  --color-accent: var(--color-neon);
  
  /* Typography */
  --font-sans: var(--font-inter), system-ui, sans-serif;
}

/* Dark-first base styles (per Design.md: Dark First Prinzip) */
html {
  scroll-behavior: smooth;
  color-scheme: dark;
}

body {
  background-color: var(--color-background);
  color: var(--color-foreground);
  font-family: var(--font-sans);
}

/* Focus states mit Akzentfarbe */
:focus-visible {
  outline: 2px solid var(--color-accent);
  outline-offset: 3px;
}
```

2. Falls tailwind.config.js/ts existiert: Loeschen (nicht mehr noetig in v4).
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/GenerationAI/website && grep -q "@theme" app/globals.css && grep -q "blue-brand" app/globals.css && echo "Tailwind v4 config OK"</automated>
  </verify>
  <done>
    - globals.css nutzt @import "tailwindcss" (v4 Syntax)
    - @theme Block definiert alle Generation AI Farben
    - Farbwelt A aktiv (blue-brand, neon, black-brand, gray-light)
    - Farbwelt B konfiguriert aber nicht als Default
    - Dark-first body styles
  </done>
</task>

<task type="auto">
  <name>Task 3: Font Setup und cn() Utility erstellen</name>
  <files>
    lib/fonts.ts
    lib/utils.ts
    app/layout.tsx
  </files>
  <action>
1. lib/fonts.ts erstellen:
```typescript
import { Inter } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-inter",
});
```

2. lib/utils.ts erstellen (shadcn Pattern):
```typescript
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

3. app/layout.tsx anpassen:
```typescript
import type { Metadata } from "next";
import { inter } from "@/lib/fonts";
import "./globals.css";

export const metadata: Metadata = {
  metadataBase: new URL("https://generation-ai.org"),
  title: {
    default: "Generation AI",
    template: "%s | Generation AI",
  },
  description: "Die erste kostenlose KI-Community fuer Studierende im DACH-Raum",
  openGraph: {
    type: "website",
    locale: "de_DE",
    siteName: "Generation AI",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="de" className={inter.variable}>
      <body className="bg-black-brand text-gray-light antialiased min-h-screen">
        {children}
      </body>
    </html>
  );
}
```
  </action>
  <verify>
    <automated>cd /Users/lucaschweigmann/projects/GenerationAI/website && test -f lib/fonts.ts && test -f lib/utils.ts && grep -q "inter.variable" app/layout.tsx && echo "Font + Utils OK"</automated>
  </verify>
  <done>
    - lib/fonts.ts exportiert inter Font mit --font-inter Variable
    - lib/utils.ts exportiert cn() Helper
    - layout.tsx importiert Font und setzt className auf html Element
    - body nutzt bg-black-brand text-gray-light (Dark-first)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local | Package installations from npm |
| next/font -> Google Fonts | Font loading from external CDN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm packages | mitigate | Use pnpm lockfile, verify package versions in package.json |
| T-01-02 | Information Disclosure | .env files | accept | No env vars in Phase 1, .gitignore includes .env* |
| T-01-03 | Denial of Service | Google Fonts | mitigate | next/font preloads and self-hosts fonts, swap fallback |
</threat_model>

<verification>
Nach Abschluss aller Tasks:

```bash
cd /Users/lucaschweigmann/projects/GenerationAI/website

# 1. Dev Server starten
pnpm dev

# 2. In neuem Terminal: Smoke Test
curl -s http://localhost:3000 | head -20

# 3. Struktur pruefen
ls -la app/ lib/ components/ 2>/dev/null
```

Manuell im Browser (localhost:3000):
- Schwarzer Hintergrund sichtbar (#141414)
- Inter Font geladen (DevTools -> Network -> Fonts)
- Keine Console Errors
</verification>

<success_criteria>
- [ ] `pnpm dev` startet ohne Fehler
- [ ] Homepage rendert mit schwarzem Hintergrund
- [ ] Tailwind Farben funktionieren (bg-blue-brand, text-neon, etc.)
- [ ] Inter Font ist geladen
- [ ] cn() Helper ist verfuegbar
- [ ] Keine TypeScript/ESLint Errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup/01-01-SUMMARY.md`
</output>
